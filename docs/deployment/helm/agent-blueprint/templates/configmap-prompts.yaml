apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-blueprint.fullname" . }}-prompts
  labels:
    {{- include "agent-blueprint.labels" . | nindent 4 }}
data:
  {{- range $name, $content := .Values.prompts }}
  {{ $name }}.prompt: |-
{{ $content | nindent 4 }}
  {{- end }}
