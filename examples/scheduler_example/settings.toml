[default]
app_name = "Scheduler Example"
app_version = "0.1.0"
app_description = "Demonstrates the Scheduler base class from Blueprint Agents"
debug = true

[default.server]
host = "0.0.0.0"
port = 8000
reload = true

[default.logging]
level = "INFO"
format = "json"
